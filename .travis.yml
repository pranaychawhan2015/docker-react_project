sudo: required
services:
  - docker

before_install: 
  - docker build -t pranaychawhan2015/docker-react-app -f Dockerfile.dev .

script: 
  - docker run -e CI=true pranaychawhan2015/docker-react-app npm run test